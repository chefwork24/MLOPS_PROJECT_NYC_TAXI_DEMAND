global:
  scrape_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "fastapi-backend"
    static_configs:
      - targets: ["host.docker.internal:8000"]
    metrics_path: "/metrics"